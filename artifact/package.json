{
  "name": "artifact",
  "version": "1.0.0",
  "description": "This directory contains the **single source of truth** for all ROME project configuration settings, eliminating conflicts between services and ensuring consistent connectivity.",
  "main": "config-loader.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.2.0"
  }
}
