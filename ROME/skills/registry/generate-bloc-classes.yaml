skill:
  name: generate-bloc-classes
  version: 1.0.0
  category: generation
  tier: 1
  description: Generate complete BLoC implementations with flutter_bloc

parameters:
  required:
    - name: design_directory
      type: string
      validation: directory_exists

    - name: output_directory
      type: string

  optional:
    - name: entities
      type: array
      default: []

execution:
  timeout: 30000

output:
  files_generated:
    type: array
  blocs_created:
    type: integer
