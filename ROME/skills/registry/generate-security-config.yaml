skill:
  name: generate-security-config
  version: 1.0.0
  category: configuration
  tier: 1
  description: Generate security configuration (Helmet, CORS, rate limiting)

parameters:
  required:
    - name: design_directory
      type: string
      validation: directory_exists

    - name: output_directory
      type: string

execution:
  timeout: 30000

output:
  files_generated:
    type: array
