skill:
  name: generate-monitoring-config
  version: 1.0.0
  category: configuration
  tier: 1
  description: Generate monitoring configuration (Prometheus metrics)

parameters:
  required:
    - name: design_directory
      type: string
      validation: directory_exists

    - name: output_directory
      type: string

execution:
  timeout: 30000

output:
  files_generated:
    type: array
