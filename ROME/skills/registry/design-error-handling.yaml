skill:
  name: design-error-handling
  version: 1.0.0
  category: design
  tier: 1
  description: Design error handling strategy and exception hierarchy

parameters:
  optional:
    - name: output_file
      type: string

execution:
  timeout: 60000

output:
  error_types:
    type: integer
  error_handling_spec:
    type: object
