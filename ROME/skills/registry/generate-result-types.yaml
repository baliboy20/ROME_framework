skill:
  name: generate-result-types
  version: 1.0.0
  category: generation
  tier: 1
  description: Generate native Dart sealed class Result<T> for error handling

parameters:
  required:
    - name: output_directory
      type: string

execution:
  timeout: 30000

output:
  files_generated:
    type: array
