ID: REQ-014

Actor: ProjectManager

Intent: approve task

Preconditions:
  - ProjectManager authenticated
  - Task exists
  - ProjectManager is project owner
  - Task status is 'pending_review'

Conditions: []

Postconditions:
  - Task status set to 'completed'
  - Task completed_at timestamp recorded
  - Task assignee notified of approval
  - Audit log entry created

Outcomes:
  - Task appears in completed tasks list
  - Task assignee receives approval notification
  - Project completion percentage updated

Invariants:
  - Approved tasks cannot be reopened
  - Completion timestamp ≥ Submission timestamp
  - Completion timestamp ≥ Created timestamp

NonFunctional:
  Performance:
    - Approval completes in <1 second
  Security:
    - Requires project owner permission
  Reliability:
    - Approval action is idempotent

Errors:
  - error: "If task status not 'pending_review'"
    message: "Only tasks pending review can be approved"
  - error: "If ProjectManager not project owner"
    message: "Only project owners can approve tasks"

ScopeBoundary:
  InScope:
    - Approve task, set to completed
  OutOfScope:
    - Conditional approval, approval with feedback

OpenQuestions: []

CopilotMode: STRICT

metadata:
  created: 2025-12-23
  author: ROME Architect
  version: 1.0
  priority: HIGH
  epic: EPIC-PILOT
  feature: FEATURE-TASK-WORKFLOW
  dependencies:
    - REQ-013
  tags:
    - advanced
    - tier-3
    - task
    - workflow
