ID: REQ-015

Actor: ProjectManager

Intent: reject task

Preconditions:
  - ProjectManager authenticated
  - Task exists
  - ProjectManager is project owner
  - Task status is 'pending_review'

Conditions:
  - Rejection reason provided (1-500 characters)

Postconditions:
  - Task status set to 'in_progress'
  - Task rejection_reason recorded
  - Task assignee notified with rejection reason
  - Audit log entry created

Outcomes:
  - Task returned to assignee for rework
  - Task assignee receives rejection notification with reason
  - Task editable by assignee again

Invariants:
  - Rejection reason cannot be empty
  - Rejection count ≥ 0 (tracked for analytics)
  - Rejected tasks return to 'in_progress' status

NonFunctional:
  Performance:
    - Rejection completes in <1 second
  Security:
    - Requires project owner permission
  Usability:
    - Clear rejection reason displayed to assignee

Errors:
  - error: "If rejection reason empty"
    message: "Rejection reason is required"
  - error: "If task status not 'pending_review'"
    message: "Only tasks pending review can be rejected"

ScopeBoundary:
  InScope:
    - Reject task with reason, return to assignee
  OutOfScope:
    - Reassign during rejection, bulk rejection

OpenQuestions:
  - question: "Should repeated rejections (>3) trigger escalation?"
    status: OPEN
    resolution: ""

CopilotMode: STRICT

metadata:
  created: 2025-12-23
  author: ROME Architect
  version: 1.0
  priority: HIGH
  epic: EPIC-PILOT
  feature: FEATURE-TASK-WORKFLOW
  dependencies:
    - REQ-013
  tags:
    - advanced
    - tier-3
    - task
    - workflow
