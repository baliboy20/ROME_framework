ID: REQ-021

Actor: SystemIntegrator

Intent: create api-token

Preconditions:
  - SystemIntegrator authenticated
  - SystemIntegrator has admin role

Conditions:
  - Token name unique within organization

Postconditions:
  - API token generated with secure random string
  - Token permissions configured
  - Token saved to database
  - Audit log entry created

Outcomes:
  - API token displayed once to SystemIntegrator
  - Token usable for API authentication
  - Token appears in active tokens list

Invariants:
  - Token string cryptographically secure (32 characters)
  - Token must have at least one permission
  - Token expiration date > Created date

NonFunctional:
  Performance:
    - Token generation completes in <1 second
  Security:
    - Token hashed before storage, displayed once only
  Reliability:
    - Token collision probability < 1 in 10^15

Errors:
  - error: "If token name already exists"
    message: "Token name already exists in your organization"
  - error: "If SystemIntegrator not admin"
    message: "Only administrators can create API tokens"

ScopeBoundary:
  InScope:
    - Generate API token with name, permissions, expiration
  OutOfScope:
    - OAuth tokens, token rotation, token scopes

OpenQuestions:
  - question: "Should tokens support IP whitelisting?"
    status: DEFERRED
    resolution: ""

CopilotMode: STRICT

metadata:
  created: 2025-12-23
  author: ROME Architect
  version: 1.0
  priority: HIGH
  epic: EPIC-PILOT
  feature: FEATURE-API-INTEGRATION
  tags:
    - integration
    - tier-4
    - api
    - security
