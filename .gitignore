# Projects are independent repos — never commit a project into the framework repo.
# (scaffolded project folders and their symlinks)
frob-admin-Bacon
frob-admin-Bacon/
.DS_Store

# Dependencies are declared in package.json and installed with npm install.
# Never commit installed packages.
node_modules/
