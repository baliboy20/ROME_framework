{
  "name": "poc-embedded-checkout-fob-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute poc-embedded-checkout-fob --local --file=./schema/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute poc-embedded-checkout-fob --remote --file=./schema/0001_init.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.67.0"
  },
  "dependencies": {
    "stripe": "^17.7.0"
  }
}
