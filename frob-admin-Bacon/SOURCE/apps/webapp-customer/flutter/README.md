# fob_webapp_customer_booking_island

Flutter Web island for the `webapp-customer` booking flow (W4-W10). See
`../README.md` for the build/publish pipeline and directory shape; this
package builds standalone (`flutter build web --release --base-href=/book/`)
and its output is copied into the static site at `en/book/flutter/`.
