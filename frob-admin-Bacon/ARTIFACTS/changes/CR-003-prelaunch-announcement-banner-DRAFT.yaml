ID: CR-003
Type: PRELAUNCH_ANNOUNCEMENT
Status: READY_TO_QUEUE
Gate:
  reviewer: Sarah
  verdict: PENDING
  date:
  notes: >-
    Not yet submitted. Sponsor resolved the blocking open questions
    2026-07-28 (OQ-1, OQ-3 below); the only remaining blocker is
    ROME-AX-19 (increment 17, unrelated in-flight work, must seal first).
Priority: HIGH
Title: "Customer web: pre-launch announcement topsheet with preview-ride application CTA"
Description: |
  The customer website (webapp-customer) should present an announcement
  overlay/banner on first open, stating that:

    - the business opens for booking in autumn 2026;
    - routes are currently being trialled;
    - members of the public are welcome to APPLY to accompany the team as a
      guest on one of the preview rides being conducted;
    - a call-to-action leads to the application.

  Requested surface: site-wide, on first open of the site (not per-page on every
  navigation). Applies across the 11 static pages under `en/` and, if the
  overlay is to persist there, the Flutter booking island.

RequestedBy: sponsor
RequestedDate: 2026-07-27

# ─────────────────────────────────────────────────────────────────────────────
# CLASSIFICATION — NOT YET VALID FOR ROUTING
# ROME-AX-31: routeChange() refuses any change whose blast radius is not
# trace-verified. The radius below is DECLARED, not verified. Do not route.
# ─────────────────────────────────────────────────────────────────────────────
ChangeType:
  declared: CT-3
  verified: false
  rationale: |
    CT-3 (requirement change), now confirmed by OQ-1's resolution: the CTA is
    a plain link to the existing delivered booking route (`en/book/`) — no new
    form, no new capability, so CT-4/increment is not needed. It is still not
    CT-2 (minor amendment / light path): no delivered requirement covers a
    site-wide pre-launch announcement surface, so this needs a new REQ plus a
    UXIS entry and a design-system overlay/scrim spec — artifact tiers above
    code. blastRadius below must still be trace-verified via impact.js before
    routing (ROME-AX-31); this file only declares it.

BlastRadius:
  verified: false
  designImpacted: true
  requirements:
    - REQ-WEB01 (home tour-showcase) — CONTRADICTION. The delivered requirement
      presents flagship published tours with live prices and deep-links into the
      booking flow. The banner simultaneously announces that booking does not
      open until autumn 2026. Both claims would render on the same page. This is
      the same contract-drift class as FINDING-002; it must be reconciled in the
      requirement, not papered over in markup.
    - REQ-PRE01 (tour catalogue) — same contradiction, catalogue surface.
    - REQ-BOOK* (booking flow entry) — the CTA target. See OQ-1.
    - REQ-CNA01 (consent capture) — engages IF the application collects contact
      details. Consent is append-only, never pre-granted, and must record its
      capture source; a new capture point is a new source.
    - NEW REQ (unassigned ID) — no delivered requirement covers a site-wide
      pre-launch announcement surface. Nothing in _requirements/ matches
      announce / banner / topsheet / coming-soon / launch.
  design:
    - _design/FOB-UXIS-001_UXIS.md — new customer-facing surface, needs a UXIS
      entry (dismissal behaviour, persistence, accessibility, reduced-motion).
    - _design/design-assets/design-system.md — overlay/scrim treatment.
    - _design/api-contracts.md — only if the application submits to an endpoint.
    NOTE: all four _design documents are project-wide (they cover all 4 apps in
    one file each) and are currently under concurrent edit by the CR-002
    session. Sequence after CR-002 lands.
  code:
    - webapp-customer/en/*.html (11 pages) + styles.css + a first-open
      suppression mechanism alongside the existing consent.js / island-loader.js
    - webapp-customer/flutter/ — only if the overlay must persist over the
      booking island

OpenQuestions:
  - id: OQ-1
    question: >-
      What does "click to apply" actually do? The request says it takes the
      applicant to booking, but applying to join a free preview ride is not the
      same act as booking a priced tour — and `en/book/` fronts a Stripe
      embedded checkout.
    status: RESOLVED
    resolution: >-
      Sponsor selects option C — the CTA routes to the existing booking flow
      (`en/book/`) as-is. Sponsor is aware this was the not-recommended option
      (a free preview-ride applicant lands in the paid Stripe checkout) and has
      accepted the risk explicitly: tour/booking wording will be revised in a
      later change to resolve the mismatch. Recorded 2026-07-28, sponsor
      directive: "the click will take you booking a tour, i will change the
      tour/booking wording down the line." No new form, no new capability —
      CTA is a plain link to the delivered booking route.
    blocking: false
  - id: OQ-2
    question: >-
      Dismissal and persistence — once per browser, once per session, or until
      launch? Affects whether any state is stored and therefore whether consent
      or cookie policy is engaged.
    status: OPEN
    blocking: false
  - id: OQ-3
    question: >-
      Does the announcement supersede or coexist with the live pricing and
      "book now" CTAs on the home page and catalogue? This is the REQ-WEB01
      reconciliation and it is a commercial decision, not a design one.
    status: RESOLVED
    resolution: >-
      Sponsor directive 2026-07-28: "ignore the contradictions" — the banner
      coexists with the live pricing/booking CTAs as delivered; REQ-WEB01/PRE01
      are not reconciled by this change. Recorded as a sponsor-accepted,
      known content inconsistency, not a defect this change must fix.
    blocking: false

Sequencing:
  - SUPERSEDED 2026-07-28: CR-002 (increment 2) sealed long ago; the project is
    now at increment 16 (CHG-015 delivered). The still-live constraint is the
    same one, against a different increment: increment 17 ("extension" intent,
    INCREMENT_BEGUN/INTAKE_FINALIZED 2026-07-28T15:19) is OPEN and unsealed at
    P0, in flight from a separate work session (CR-006..CR-013 territory).
    state.js#beginChange throws while a prior increment is unsealed
    (ROME-AX-19); CR-003 cannot start its own change-scoped increment until
    increment 17 seals. Do not force through it.
  - Content/scope blockers are CLEARED (OQ-1, OQ-3 resolved by sponsor
    2026-07-28 — see OpenQuestions). Only OQ-2 (dismissal/persistence) remains
    open and is non-blocking. CR-003 is READY_TO_QUEUE, pending increment 17.

CopilotMode: STRICT
