# Migration Log — semantics ledger (ROME-PROP-055 Part C)

Agents reading pre-migration artifacts MUST consult this before flagging or "fixing" them.

## 3.2.1 → 3.3.0 (applied 2026-07-27T14:06:55Z)
- Pre-3.3.0 projects have no change queue: amendments made before this level (if any) were routed as full increments — historical fact, not a violation.
- The intent labels (`refinement`/`extension`/`migration`) on old increments predate the CT taxonomy; do not re-map them retroactively.
